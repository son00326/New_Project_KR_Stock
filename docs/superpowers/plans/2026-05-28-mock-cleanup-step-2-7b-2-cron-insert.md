# Mock cleanup Step 2.7b.2 — Cron INSERT 실 path Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** silent-health cron의 `heartbeat_log` INSERT + news-sweep cron의 `news_event` INSERT를 실 path로 가동(service-role client 주입). 두 cron 모두 idempotent (date / url unique 기반).

**Architecture:**
- (1) silent-health: `tudal/src/lib/data/admin-heartbeat-log.ts` 신규 helper + DI seam (`options.client?`) + ON CONFLICT (date) DO UPDATE upsert (cron 재실행/D10 catch-up 시 latest status로 갱신).
- (2) news-sweep: `tudal/src/lib/data/admin-news.ts` 기존 helper에 `insertNewsEvents()` 추가 + batch upsert + ON CONFLICT (url) DO NOTHING (append-only news, deterministic classifier).
- (3) 두 route 모두 INSERT 실패 시 5xx 응답 + audit body (cron 재실행은 Vercel cron 자체 cadence 의존, INSERT 자체 retry 없음).
- (4) 0 migrations — `heartbeat_log` schema는 0008 §2, `news_event` schema는 0006 §2 production 적용 완료.

**Tech Stack:** Next.js 16 App Router · @supabase/supabase-js · service-role client (PR1 B17 boundary 확장 없음 — silent-health/news-sweep 이미 등록) · Vitest TDD.

---

## §0 Architectural Decisions (omxy R-debate scope guard)

### D1. heartbeat_log idempotency = ON CONFLICT (date) DO UPDATE (upsert)
- **Why**: cron 일 1회 발송(15:00 UTC) + D10 catch-up retry + 수동 trigger 시 같은 `date`에 multiple write 발생 가능. 첫 write가 `send_failed=true`였는데 catch-up이 성공 → 최신 상태로 갱신해야 dashboard truth 정합.
- **Update columns**: status, generated_at, pipeline_summary, critical_alert_count, warning_alert_count, sent_channels, send_failed, message.
- **Invariant**: `date`(UNIQUE) 키 보존, `id`(uuid PK) auto-generated by default (conflict path는 기존 id 유지).
- **Reject 대안 B (DO NOTHING)**: 첫 fail row가 영구 잔존 → false positive alert.
- **Reject 대안 C (INSERT only + duplicate error catch)**: error noise + RPC 안 쓰면 atomic 보장 부재.

### D2. news_event idempotency = ON CONFLICT (url) DO NOTHING (`.upsert(..., { onConflict: 'url', ignoreDuplicates: true })`)
- **Why**: news는 append-only event. classifier (`classifyNews`) 결과는 title 기준 deterministic → 동일 URL 재분류 = 같은 결과. 갱신 의미 없음.
- **batch single-call**: classifier output `classified[]`을 batch insert (single transaction, partial-failure 없음).
- **Reject 대안 B (DO UPDATE)**: classifier deterministic 시 무의미 write + audit trail 혼란.
- **Reject 대안 C (per-row insert + dedupe)**: round-trips × N.

### D3. Retry / failure 처리
- **heartbeat_log INSERT 실패** → response status 502 + audit body에 `dbError` 명시. `sendFailed` 응답 필드는 발송 실패와 분리(DB write 실패는 별개 신호).
  - "발송 성공 + INSERT 실패" 시 응답 status는 502 (audit trail loss는 critical signal).
  - "발송 실패 + INSERT 성공" 시 응답 status는 기존 502 유지 (sendFailed=true).
- **news_event INSERT 실패** → response status 502 + audit body에 `dbError`. classifier 결과는 응답에 포함되지만 적재 미완.
- **Outer retry**: Vercel cron 자체 daily cadence (silent-health 15:00 UTC + news-sweep 00:00 UTC). INSERT 자체 retry 없음.

### D3b. news-sweep WRITE path 일관성 (omxy R1 HIGH-1 fix + R2 MED-2 lazy-create refinement)
- live Naver mode (`naverOn=true`)에서도 `insertNewsEvents` 호출. classifier 출력은 항상 `classified[]` → 항상 WRITE 시도.
- **service-role client는 lazy create** (R2 MED-2): branch 전 top-level 생성 금지 (live Naver fetch 실패 / partial failure 시에는 DB WRITE 없음 → service-role env/cache 건드릴 필요 없음).
  - mock branch (non-production + Naver 키 없음): `getRecentNewsEvents` SELECT 직전 `createServiceRoleClient()` 호출. 동일 client 직후 INSERT 재사용.
  - live branch (`naverOn=true`): Naver fetch 성공 + `classified` 생성 후 INSERT 직전 `createServiceRoleClient()` 호출. Naver 실패 시 호출 0건.
- mock-mode fallback에서는 service-role client로 `getRecentNewsEvents` SELECT + `classified`을 재분류하여 INSERT (대부분 ON CONFLICT (url) DO NOTHING).

### D4. Migration = 0
- `heartbeat_log` schema 0008 §2 (production 적용 완료, `0001~0025` chain).
- `news_event` schema 0006 §2 (production 적용 완료).
- RLS는 `for all to authenticated using(public.is_admin())` → service-role client는 RLS 우회 (cron context 정합).

### D5. Helper boundary
- **silent-health**: `tudal/src/lib/data/admin-heartbeat-log.ts` 신규 (READ+WRITE 같은 도메인 명시).
  - `insertHeartbeatLog(record, { client? })`: ON CONFLICT (date) DO UPDATE.
- **news-sweep**: `tudal/src/lib/data/admin-news.ts` 기존 helper 확장.
  - `insertNewsEvents(events, { client? })`: ON CONFLICT (url) DO NOTHING. (정합: getRecentNewsEvents 옆 export.)

### D6. service-role boundary
- 0 신규 boundary (cron/silent-health + cron/news-sweep 모두 Step 2.7a / 2.7b.1에서 등록 완료).
- `tudal/src/lib/supabase/service-role.ts` 주석에 INSERT 추가 (`Step 2.7b.2`).
- DI seam helpers 허용 목록에 `admin-heartbeat-log.ts` 추가.

### D7. Scope-out (W-defer 신규 ticket)
- **W-cron-alert-event-insert** (Step 2.7b.3 후속) — silent-health `missingAlert` → alert_event INSERT + news-sweep `alerts[]` → alert_event INSERT + morning-briefing `alertPayload` → alert_event INSERT. 3-source 동일 sink로 통일 후 별도 step.
- **W-briefing-log-insert** (Step 2.7b.3 후속) — morning-briefing `briefing_log` INSERT (line 111 TODO 해소).
- **W-portfolio-snapshot-real** (S7b 트랙) — morning-briefing `portfolioSnapshot: null` 실 SELECT.

---

## §1 File Structure (touched files lock-in)

**Create**:
- `tudal/src/lib/data/admin-heartbeat-log.ts` — heartbeat_log INSERT helper + DI seam.
- `tudal/src/lib/data/__tests__/admin-heartbeat-log.test.ts` — TDD invariants.
- `tudal/src/lib/data/__tests__/admin-news-insert.test.ts` — TDD invariants (insertNewsEvents).

**Modify**:
- `tudal/src/lib/data/admin-news.ts` — `insertNewsEvents` export 추가.
- `tudal/src/app/api/cron/silent-health/route.ts` — `insertHeartbeatLog` 호출 wiring (line 146 `const log = toHeartbeatLogRecord(...)` 직후 INSERT). `dbError` 응답 필드 추가.
- `tudal/src/app/api/cron/news-sweep/route.ts` — `insertNewsEvents` 호출 wiring (line 124 `classified` 생성 직후 INSERT). `dbError` 응답 필드 추가.
- `tudal/src/lib/supabase/service-role.ts` — 주석 boundary에 `admin-heartbeat-log.ts` (DI seam helpers) 추가.

**No new tests directly in cron route**: route INSERT 호출은 helper level TDD에서 검증 + 신규 route-level test는 INSERT call assertion(DI seam invariant) + dbError 5xx 응답.

**Modify route tests (해당 시)**:
- `tudal/src/app/api/cron/silent-health/__tests__/*.test.ts` — INSERT mock 추가 + dbError path.
- `tudal/src/app/api/cron/news-sweep/__tests__/*.test.ts` — INSERT mock 추가 + dbError path.

---

## §2 Tasks

### Task 1: admin-heartbeat-log.ts 신규 helper + TDD invariants

**Files:**
- Create: `tudal/src/lib/data/admin-heartbeat-log.ts`
- Test: `tudal/src/lib/data/__tests__/admin-heartbeat-log.test.ts`

- [ ] **Step 1.1: Write failing tests (TDD red)**

```typescript
// tudal/src/lib/data/__tests__/admin-heartbeat-log.test.ts
import { describe, it, expect, vi } from "vitest";
import type { SupabaseClient } from "@supabase/supabase-js";

describe("insertHeartbeatLog", () => {
  const validRecord = {
    date: "2026-05-27",
    status: "ok" as const,
    generatedAt: "2026-05-28T15:00:00.000Z",
    pipelineSummary: [
      { pipeline: "dart", successRate: 1, severity: "info" as const },
    ],
    criticalAlertCount: 0,
    warningAlertCount: 0,
    sentChannels: ["dashboard", "telegram"],
    sendFailed: false,
    message: "✅ [주픽] 2026-05-27 — 오늘 이상 없음",
  };

  it("camelCase → snake_case mapping + upsert(date) ignoreDuplicates=false", async () => {
    const upsertMock = vi.fn().mockResolvedValue({ error: null });
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    const client = { from: fromMock } as unknown as SupabaseClient;

    const { insertHeartbeatLog } = await import("@/lib/data/admin-heartbeat-log");
    await insertHeartbeatLog(validRecord, { client });

    expect(fromMock).toHaveBeenCalledWith("heartbeat_log");
    expect(upsertMock).toHaveBeenCalledWith(
      expect.objectContaining({
        date: "2026-05-27",
        status: "ok",
        generated_at: "2026-05-28T15:00:00.000Z",
        pipeline_summary: validRecord.pipelineSummary,
        critical_alert_count: 0,
        warning_alert_count: 0,
        sent_channels: ["dashboard", "telegram"],
        send_failed: false,
        message: validRecord.message,
      }),
      { onConflict: "date" },
    );
  });

  it("DI seam fallback (no client) uses session createClient (omxy R1 MED-1 fix)", async () => {
    vi.resetModules();
    const upsertMock = vi.fn().mockResolvedValue({ error: null });
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    vi.doMock("@/lib/supabase/server", () => ({
      createClient: vi.fn().mockResolvedValue({ from: fromMock }),
    }));

    const { insertHeartbeatLog } = await import("@/lib/data/admin-heartbeat-log");
    await insertHeartbeatLog(validRecord);

    expect(fromMock).toHaveBeenCalledWith("heartbeat_log");
    expect(upsertMock).toHaveBeenCalledTimes(1);
  });

  it("error → throws heartbeat_log_insert_failed:<code>", async () => {
    const upsertMock = vi.fn().mockResolvedValue({
      error: { code: "23505", message: "duplicate" },
    });
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    const client = { from: fromMock } as unknown as SupabaseClient;

    const { insertHeartbeatLog } = await import("@/lib/data/admin-heartbeat-log");
    await expect(insertHeartbeatLog(validRecord, { client })).rejects.toThrow(
      "heartbeat_log_insert_failed:23505",
    );
  });

  it("status enum guard rejects unknown status", async () => {
    const upsertMock = vi.fn().mockResolvedValue({ error: null });
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    const client = { from: fromMock } as unknown as SupabaseClient;

    const { insertHeartbeatLog } = await import("@/lib/data/admin-heartbeat-log");
    await expect(
      insertHeartbeatLog(
        { ...validRecord, status: "weird" as never },
        { client },
      ),
    ).rejects.toThrow("heartbeat_log_invalid_status:weird");
    expect(upsertMock).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 1.2: Run tests to verify they fail**

Run: `cd tudal && npx vitest run src/lib/data/__tests__/admin-heartbeat-log.test.ts`
Expected: FAIL (module not found / not exported).

- [ ] **Step 1.3: Implement insertHeartbeatLog**

```typescript
// tudal/src/lib/data/admin-heartbeat-log.ts
// heartbeat_log 테이블 INSERT helper (59차 Mock cleanup Step 2.7b.2).
//
// silent-health cron이 일 1회 (15:00 UTC) 호출. ON CONFLICT (date) DO UPDATE upsert로
// catch-up retry / 수동 trigger / D10 재시도 시 latest status 반영. RLS는 admin all →
// cron context (admin cookie 없음)는 service-role client 주입 필수.
//
// SoT: 0008_s6_hardening.sql §heartbeat_log (date UNIQUE + status check + admin RLS).

import type { SupabaseClient } from "@supabase/supabase-js";
import { createClient } from "@/lib/supabase/server";
import type { HeartbeatLog, HeartbeatStatus } from "@/types/admin";

const STATUS_SET: ReadonlySet<HeartbeatStatus> = new Set<HeartbeatStatus>([
  "ok",
  "red_alert",
]);

export interface HeartbeatLogInsertOptions {
  client?: SupabaseClient;
}

export async function insertHeartbeatLog(
  record: Omit<HeartbeatLog, "id">,
  options: HeartbeatLogInsertOptions = {},
): Promise<void> {
  if (!STATUS_SET.has(record.status)) {
    throw new Error(`heartbeat_log_invalid_status:${record.status}`);
  }
  const supabase = options.client ?? (await createClient());
  const { error } = await supabase
    .from("heartbeat_log")
    .upsert(
      {
        date: record.date,
        status: record.status,
        generated_at: record.generatedAt,
        pipeline_summary: record.pipelineSummary,
        critical_alert_count: record.criticalAlertCount,
        warning_alert_count: record.warningAlertCount,
        sent_channels: record.sentChannels,
        send_failed: record.sendFailed,
        message: record.message,
      },
      { onConflict: "date" },
    );
  if (error) {
    throw new Error(`heartbeat_log_insert_failed:${error.code ?? "unknown"}`);
  }
}
```

- [ ] **Step 1.4: Run tests to verify they pass**

Run: `cd tudal && npx vitest run src/lib/data/__tests__/admin-heartbeat-log.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 1.5: Commit**

```bash
git add tudal/src/lib/data/admin-heartbeat-log.ts tudal/src/lib/data/__tests__/admin-heartbeat-log.test.ts
git commit -m "feat(mock-cleanup step 2.7b.2): insertHeartbeatLog helper + DI seam + status guard"
```

---

### Task 2: insertNewsEvents 확장 + TDD invariants

**Files:**
- Modify: `tudal/src/lib/data/admin-news.ts`
- Test: `tudal/src/lib/data/__tests__/admin-news-insert.test.ts`

- [ ] **Step 2.1: Write failing tests (TDD red)**

```typescript
// tudal/src/lib/data/__tests__/admin-news-insert.test.ts
import { describe, it, expect, vi } from "vitest";
import type { SupabaseClient } from "@supabase/supabase-js";
import type { NewsEvent } from "@/types/admin";

describe("insertNewsEvents", () => {
  const events: NewsEvent[] = [
    {
      id: "sweep-1",
      ticker: "005930",
      severity: "critical",
      title: "삼성전자 매출 급락",
      source: "Naver",
      url: "https://news.naver.com/a",
      publishedAt: "2026-05-28T00:00:00.000Z",
      fetchedAt: "2026-05-28T00:01:00.000Z",
      classificationReason: "매출 급락 → critical",
    },
    {
      id: "sweep-2",
      ticker: null,
      severity: "info",
      title: "시장 보합",
      source: "Naver",
      url: "https://news.naver.com/b",
      publishedAt: "2026-05-28T00:00:00.000Z",
      fetchedAt: "2026-05-28T00:01:00.000Z",
      classificationReason: null,
    },
  ];

  it("batch upsert(url) ignoreDuplicates=true + camelCase → snake_case", async () => {
    const upsertMock = vi.fn().mockResolvedValue({ error: null });
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    const client = { from: fromMock } as unknown as SupabaseClient;

    const { insertNewsEvents } = await import("@/lib/data/admin-news");
    await insertNewsEvents(events, { client });

    expect(fromMock).toHaveBeenCalledWith("news_event");
    expect(upsertMock).toHaveBeenCalledTimes(1);
    const [rows, opts] = upsertMock.mock.calls[0];
    expect(rows).toHaveLength(2);
    expect(rows[0]).toEqual(
      expect.objectContaining({
        ticker: "005930",
        severity: "critical",
        title: "삼성전자 매출 급락",
        source: "Naver",
        url: "https://news.naver.com/a",
        published_at: "2026-05-28T00:00:00.000Z",
        fetched_at: "2026-05-28T00:01:00.000Z",
        classification_reason: "매출 급락 → critical",
      }),
    );
    expect(rows[1]).toEqual(
      expect.objectContaining({ ticker: null, classification_reason: null }),
    );
    expect(opts).toEqual({ onConflict: "url", ignoreDuplicates: true });
  });

  it("empty array → no DB call, no throw", async () => {
    const upsertMock = vi.fn();
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    const client = { from: fromMock } as unknown as SupabaseClient;

    const { insertNewsEvents } = await import("@/lib/data/admin-news");
    await insertNewsEvents([], { client });

    expect(fromMock).not.toHaveBeenCalled();
    expect(upsertMock).not.toHaveBeenCalled();
  });

  it("severity enum guard rejects invalid row", async () => {
    const upsertMock = vi.fn().mockResolvedValue({ error: null });
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    const client = { from: fromMock } as unknown as SupabaseClient;

    const { insertNewsEvents } = await import("@/lib/data/admin-news");
    await expect(
      insertNewsEvents(
        [{ ...events[0], severity: "weird" as never }],
        { client },
      ),
    ).rejects.toThrow("news_event_invalid_severity:weird");
    expect(upsertMock).not.toHaveBeenCalled();
  });

  it("error → throws news_event_insert_failed:<code>", async () => {
    const upsertMock = vi.fn().mockResolvedValue({
      error: { code: "23502", message: "null violates" },
    });
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    const client = { from: fromMock } as unknown as SupabaseClient;

    const { insertNewsEvents } = await import("@/lib/data/admin-news");
    await expect(insertNewsEvents(events, { client })).rejects.toThrow(
      "news_event_insert_failed:23502",
    );
  });

  it("DI seam fallback (no client) uses session createClient", async () => {
    vi.resetModules();
    const upsertMock = vi.fn().mockResolvedValue({ error: null });
    const fromMock = vi.fn().mockReturnValue({ upsert: upsertMock });
    vi.doMock("@/lib/supabase/server", () => ({
      createClient: vi.fn().mockResolvedValue({ from: fromMock }),
    }));

    const { insertNewsEvents } = await import("@/lib/data/admin-news");
    await insertNewsEvents(events);

    expect(fromMock).toHaveBeenCalledWith("news_event");
    expect(upsertMock).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2.2: Run tests to verify they fail**

Run: `cd tudal && npx vitest run src/lib/data/__tests__/admin-news-insert.test.ts`
Expected: FAIL (`insertNewsEvents` not exported).

- [ ] **Step 2.3: Implement insertNewsEvents in admin-news.ts**

Append to `tudal/src/lib/data/admin-news.ts` (after line 99 `getRecentNewsEvents`):

```typescript
// 59차 Mock cleanup Step 2.7b.2: news-sweep cron classifier 결과를 news_event 테이블에 batch
// upsert. ON CONFLICT (url) DO NOTHING (news_event_url_uniq) — append-only news, classifier
// deterministic → 동일 URL 재분류 무의미. service-role client 주입 시 RLS using(is_admin()) 우회.

export async function insertNewsEvents(
  events: NewsEvent[],
  options: { client?: SupabaseClient } = {},
): Promise<void> {
  if (events.length === 0) return;
  for (const e of events) {
    if (!SEVERITY_SET.has(e.severity)) {
      throw new Error(`news_event_invalid_severity:${e.severity}`);
    }
  }
  const supabase = options.client ?? (await createClient());
  const rows = events.map((e) => ({
    ticker: e.ticker,
    severity: e.severity,
    title: e.title,
    source: e.source,
    url: e.url,
    published_at: e.publishedAt,
    fetched_at: e.fetchedAt,
    classification_reason: e.classificationReason,
  }));
  const { error } = await supabase
    .from("news_event")
    .upsert(rows, { onConflict: "url", ignoreDuplicates: true });
  if (error) {
    throw new Error(`news_event_insert_failed:${error.code ?? "unknown"}`);
  }
}
```

- [ ] **Step 2.4: Run tests to verify they pass**

Run: `cd tudal && npx vitest run src/lib/data/__tests__/admin-news-insert.test.ts`
Expected: PASS (5 tests).

- [ ] **Step 2.5: Commit**

```bash
git add tudal/src/lib/data/admin-news.ts tudal/src/lib/data/__tests__/admin-news-insert.test.ts
git commit -m "feat(mock-cleanup step 2.7b.2): insertNewsEvents batch upsert + DI seam"
```

---

### Task 3: silent-health route INSERT wiring

**Files:**
- Modify: `tudal/src/app/api/cron/silent-health/route.ts`
- Modify: `tudal/src/lib/supabase/service-role.ts` (boundary 주석 갱신)
- Test: 신규 또는 기존 `tudal/src/app/api/cron/silent-health/__tests__/route.test.ts` (이미 존재 시 확장)

- [ ] **Step 3.1: Locate existing silent-health route test**

Run: `find tudal/src/app/api/cron/silent-health -name "*.test.ts"`

Expected: 기존 route test 발견 (Step 2.7a R1에서 4-way auth + DI invariant 추가됨). 없으면 신규 작성.

- [ ] **Step 3.2: Write failing test — INSERT call assertion**

기존 test 파일 (Step 2.7a가 도입한 4-way auth/DI invariant 보유)에 다음 패턴 적용 (omxy R4 HIGH-1 + MED-1 fix):

**(1) hoist-safe mock 등록 (R4 HIGH-1)** — 기존 file의 `vi.hoisted` 패턴 정합. file 상단 (다른 hoisted mocks 인접):

```typescript
// silent-health/__tests__/*.test.ts 상단 — 기존 hoisted mocks 옆에 추가:
const heartbeatLogMock = vi.hoisted(() => ({
  insertHeartbeatLog: vi.fn(),
}));

vi.mock("@/lib/data/admin-heartbeat-log", () => ({
  insertHeartbeatLog: heartbeatLogMock.insertHeartbeatLog,
}));
```

**(2) 신규 describe block — 기존 describe 내부 또는 sibling 동일 mock setup 명시 (R4 MED-1)**:
- 기존 file의 `describe("GET /api/cron/silent-health", ...)` 내부에 추가하면 `beforeEach` (serviceRoleMock / getRecentPipelineHealth / getRecentAlertEvents 기본값) 자동 상속. **권장 path**.
- sibling describe로 분리 시 동일 beforeEach setup 복제 필수 (mock 누락 시 flaky).

```typescript
// 기존 describe 내부 (또는 sibling + setup 복제) — omxy R1 HIGH-3 정합 (vi.stubEnv 패턴):
import { NextRequest } from "next/server";

describe("INSERT wiring (Step 2.7b.2)", () => {
  beforeEach(() => {
    heartbeatLogMock.insertHeartbeatLog.mockReset();
    heartbeatLogMock.insertHeartbeatLog.mockResolvedValue(undefined);
    // ⚠️ 기존 describe 내부 적용 시: 부모 beforeEach가 vi.stubEnv("CRON_SECRET", ...) /
    // serviceRoleMock / getRecentPipelineHealth / getRecentAlertEvents 모두 세팅 보장 → 본 nested
    // beforeEach는 insertHeartbeatLog mock 추가 setup만 담당.
    //
    // sibling describe 적용 시: 부모 beforeEach 미상속 → 다음 setup 명시:
    //   vi.stubEnv("CRON_SECRET", ""); vi.stubEnv("VERCEL_ENV", ""); vi.stubEnv("NEXT_PUBLIC_APP_ENV", "");
    //   vi.stubEnv("NODE_ENV", "test");
    //   serviceRoleMock.createServiceRoleClient.mockReturnValue(serviceRoleMock.client as never);
    //   pipelineHealthMock.getRecentPipelineHealth.mockResolvedValue([]);
    //   alertsMock.getRecentAlertEvents.mockResolvedValue([]);
  });

  it("INSERT called with toHeartbeatLogRecord output + service-role client", async () => {
    const { GET } = await import("../route");
    const res = await GET(
      new NextRequest("https://example.com/api/cron/silent-health"),
    );
    expect(res.status).toBe(200);
    expect(heartbeatLogMock.insertHeartbeatLog).toHaveBeenCalledTimes(1);
    const [record, opts] = heartbeatLogMock.insertHeartbeatLog.mock.calls[0];
    expect(record).toEqual(
      expect.objectContaining({
        date: expect.stringMatching(/^\d{4}-\d{2}-\d{2}$/),
        status: expect.stringMatching(/^(ok|red_alert)$/),
        message: expect.any(String),
      }),
    );
    expect(opts).toHaveProperty("client");
    expect(opts.client).toBeDefined();
  });

  it("INSERT fail → 502 + dbError audit body, response still includes log", async () => {
    heartbeatLogMock.insertHeartbeatLog.mockRejectedValue(
      new Error("heartbeat_log_insert_failed:23505"),
    );
    const { GET } = await import("../route");
    const res = await GET(
      new NextRequest("https://example.com/api/cron/silent-health"),
    );
    expect(res.status).toBe(502);
    const body = await res.json();
    expect(body.ok).toBe(false);
    expect(body.dbError).toBe("heartbeat_log_insert_failed:23505");
    expect(body.log).toBeDefined();
  });
});
```

- [ ] **Step 3.3: Run tests to verify they fail**

Run: `cd tudal && npx vitest run src/app/api/cron/silent-health/__tests__/`
Expected: FAIL (insertHeartbeatLog not wired).

- [ ] **Step 3.4: Wire insertHeartbeatLog in route**

Edit `tudal/src/app/api/cron/silent-health/route.ts`:

(a) Add import at top:
```typescript
import { insertHeartbeatLog } from "@/lib/data/admin-heartbeat-log";
```

(b) After line 146 (`const log = toHeartbeatLogRecord(...)`), insert INSERT call (before line 156 `if (allFailed)`):

```typescript
  // Step 2.7b.2: heartbeat_log 실 INSERT (service-role 단일 인스턴스 재사용 — D5 boundary).
  // ON CONFLICT (date) DO UPDATE — cron 재실행 / D10 catch-up retry / 수동 trigger 시 latest
  // status로 갱신 (D1). INSERT 실패 = audit trail loss → 5xx + dbError audit body (D3).
  let dbError: string | null = null;
  try {
    await insertHeartbeatLog(log, { client: serviceRoleClient });
  } catch (err) {
    dbError = err instanceof Error ? err.message : "heartbeat_log_insert_failed:unknown";
  }
```

(c) Response body / status 갱신:

```typescript
  // dbError가 있으면 5xx (audit trail loss). 기존 noConfiguredOutboundChannel(500) /
  // sendFailed(502) 우선순위 유지. dbError는 그 다음 502.
  const finalStatus = noConfiguredOutboundChannel
    ? 500
    : sendFailed
    ? 502
    : dbError
    ? 502
    : 200;

  return NextResponse.json(
    {
      ok: !sendFailed && !dbError,
      date,
      status: classification.status,
      sentChannels,
      d10Triggered,
      log,
      alertEmitted: missingAlert?.triggerReason ?? null,
      dbError,
    },
    { status: finalStatus },
  );
```

(d) Replace original return block at lines 163-174 with above.

- [ ] **Step 3.5: Update service-role.ts boundary comment**

Edit `tudal/src/lib/supabase/service-role.ts` boundary section (line 5-13):

```typescript
//   허용 (cron route 직접 import + DI seam helpers를 통한 간접 사용):
//     - tudal/src/app/api/cron/monthly-batch/** (PR1 B17 원본)
//     - tudal/src/app/api/cron/silent-health/** (Step 2.7a + INSERT Step 2.7b.2)
//     - tudal/src/app/api/cron/news-sweep/** (Step 2.7b.1 + INSERT Step 2.7b.2)
//     - tudal/src/app/api/cron/morning-briefing/** (Step 2.7b.1)
//     - tudal/src/lib/data/admin-batch-runs-cron.ts (PR1 B17 원본)
//   허용 (DI seam을 통한 cron 호출자 service-role 주입 — admin pages는 session client 유지):
//     - tudal/src/lib/data/admin-news.ts (Step 2.7a: options.client? + Step 2.7b.2: INSERT)
//     - tudal/src/lib/data/admin-alerts.ts (Step 2.7a: options.client?)
//     - tudal/src/lib/data/admin-pipeline-health.ts (Step 2.5 기존 options.client?)
//     - tudal/src/lib/data/admin-heartbeat-log.ts (Step 2.7b.2: options.client? INSERT)
```

- [ ] **Step 3.6: Run tests to verify they pass**

Run: `cd tudal && npx vitest run src/app/api/cron/silent-health/__tests__/`
Expected: PASS (기존 + 신규 2 tests).

- [ ] **Step 3.7: Commit**

```bash
git add tudal/src/app/api/cron/silent-health/route.ts tudal/src/lib/supabase/service-role.ts tudal/src/app/api/cron/silent-health/__tests__/
git commit -m "feat(mock-cleanup step 2.7b.2): silent-health heartbeat_log INSERT wiring + 502 dbError"
```

---

### Task 4: news-sweep route INSERT wiring

**Files:**
- Modify: `tudal/src/app/api/cron/news-sweep/route.ts`
- Modify (existing test rename, omxy R1 HIGH-1 fix): `tudal/src/app/api/cron/news-sweep/__tests__/route.test.ts` line 131 test `"does not create a service-role read client in live Naver mode"` → 의미 변경 (live mode는 read 안 함 + WRITE 위해 service-role 1회 생성) → test rename + 호출 1회 허용 + READ 호출 0회 단언으로 교체:
  ```typescript
  it("creates service-role client once in live Naver mode (WRITE only, no READ call)", async () => {
    // ... 기존 live mode setup ...
    // After GET():
    expect(serviceRoleMock.createServiceRoleClient).toHaveBeenCalledTimes(1);
    expect(adminNewsMock.getRecentNewsEvents).not.toHaveBeenCalled(); // live mode는 READ 안 함
  });
  ```
- Test (신규 describe block): 본 plan Task 4.1

- [ ] **Step 4.1: Write failing test — INSERT call assertion**

**Critical (omxy R1 HIGH-2 fix)**: 기존 `route.test.ts`의 `vi.hoisted` 블록(`adminNewsMock.getRecentNewsEvents`)에 `insertNewsEvents`를 추가하여 single block 유지. 두 번째 `vi.mock` 등록 시도 시 vitest hoisting 중복 → silent override. 기존 file의 hoisted 패턴 정확 매치:

```typescript
// 기존 (route.test.ts:7-10)
const adminNewsMock = vi.hoisted(() => ({
  getRecentNewsEvents: vi.fn(),
  insertNewsEvents: vi.fn(), // ← Step 2.7b.2 추가
}));

vi.mock("@/lib/data/admin-news", () => ({
  getRecentNewsEvents: adminNewsMock.getRecentNewsEvents,
  insertNewsEvents: adminNewsMock.insertNewsEvents, // ← 추가
}));
```

신규 describe 블록 (CRON_SECRET handling omxy R1 HIGH-3 fix + HIGH-1 fix live/mock 양쪽 INSERT 검증):

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { NextRequest } from "next/server";

describe("news-sweep INSERT wiring (Step 2.7b.2)", () => {
  beforeEach(() => {
    adminNewsMock.insertNewsEvents.mockReset();
    adminNewsMock.getRecentNewsEvents.mockResolvedValue([]); // 빈 fallback
    naverMock.fetchNaverNews.mockReset();
    serviceRoleMock.createServiceRoleClient.mockReset();
    serviceRoleMock.createServiceRoleClient.mockReturnValue(
      serviceRoleMock.client as never,
    );
    // omxy R1 HIGH-3 + R3 hint fix — vi.stubEnv는 기존 file 패턴 정합 (afterEach 자동 restore).
    vi.stubEnv("CRON_SECRET", "");
    vi.stubEnv("VERCEL_ENV", "");
    vi.stubEnv("NEXT_PUBLIC_APP_ENV", "");
    vi.stubEnv("NODE_ENV", "test");
    vi.stubEnv("NAVER_CLIENT_ID", "");
    vi.stubEnv("NAVER_CLIENT_SECRET", "");
  });

  afterEach(() => {
    vi.unstubAllEnvs(); // 기존 file 패턴 정합
  });

  it("mock-mode (Naver keys missing): classified events INSERT with service-role client", async () => {
    adminNewsMock.insertNewsEvents.mockResolvedValue(undefined);
    const { GET } = await import("../route");
    const res = await GET(
      new NextRequest("https://example.com/api/cron/news-sweep"),
    );
    expect(res.status).toBe(200);
    expect(adminNewsMock.insertNewsEvents).toHaveBeenCalledTimes(1);
    const [events, opts] = adminNewsMock.insertNewsEvents.mock.calls[0];
    expect(Array.isArray(events)).toBe(true);
    expect(opts).toHaveProperty("client");
    expect(opts.client).toBeDefined();
  });

  it("live-mode (Naver keys present + fetchNaverNews success): INSERT called once + service-role created once + READ never (omxy R1 HIGH-1 + R2 MED-2)", async () => {
    vi.stubEnv("NAVER_CLIENT_ID", "test-id");
    vi.stubEnv("NAVER_CLIENT_SECRET", "test-secret");
    naverMock.fetchNaverNews.mockResolvedValue([]); // 빈 fetch 결과 OK
    adminNewsMock.insertNewsEvents.mockResolvedValue(undefined);
    const { GET } = await import("../route");
    const res = await GET(
      new NextRequest("https://example.com/api/cron/news-sweep"),
    );
    expect(res.status).toBe(200);
    expect(adminNewsMock.insertNewsEvents).toHaveBeenCalledTimes(1);
    expect(serviceRoleMock.createServiceRoleClient).toHaveBeenCalledTimes(1);
    expect(adminNewsMock.getRecentNewsEvents).not.toHaveBeenCalled(); // live mode READ 안 함
  });

  it("live-mode Naver all-fail → 502 early return + insertNewsEvents NOT called + service-role NOT created (omxy R2 MED-2)", async () => {
    vi.stubEnv("NAVER_CLIENT_ID", "test-id");
    vi.stubEnv("NAVER_CLIENT_SECRET", "test-secret");
    // 기존 file의 naverMock hoisted 패턴 정합 — reject로 override.
    naverMock.fetchNaverNews.mockRejectedValue(new Error("naver_429"));
    const { GET } = await import("../route");
    const res = await GET(
      new NextRequest("https://example.com/api/cron/news-sweep"),
    );
    expect(res.status).toBe(502);
    expect(adminNewsMock.insertNewsEvents).not.toHaveBeenCalled();
    expect(serviceRoleMock.createServiceRoleClient).not.toHaveBeenCalled();
  });

  it("INSERT fail → 502 + dbError, alertsEmitted=0 + classified preserved", async () => {
    adminNewsMock.insertNewsEvents.mockRejectedValue(
      new Error("news_event_insert_failed:23502"),
    );
    const { GET } = await import("../route");
    const res = await GET(
      new NextRequest("https://example.com/api/cron/news-sweep"),
    );
    expect(res.status).toBe(502);
    const body = await res.json();
    expect(body.ok).toBe(false);
    expect(body.dbError).toBe("news_event_insert_failed:23502");
    expect(body.classified).toBeDefined();
    expect(body.alertsEmitted).toBe(0);
  });
});
```

- [ ] **Step 4.2: Run tests to verify they fail**

Run: `cd tudal && npx vitest run src/app/api/cron/news-sweep/__tests__/`
Expected: FAIL.

- [ ] **Step 4.3: Wire insertNewsEvents in route**

Edit `tudal/src/app/api/cron/news-sweep/route.ts`:

(a) Import update (line 2):
```typescript
import { getRecentNewsEvents, insertNewsEvents } from "@/lib/data/admin-news";
```

(b) Refactor handler — service-role client는 **lazy create per branch** (R2 MED-2 fix). 단일 변수 `serviceRoleClient: SupabaseClient | null = null` declare + branch 내부에서 필요 시 1회 lazy 생성. live branch는 Naver fetch 성공 후 INSERT 직전에만 생성 (실패/partial failure path는 호출 0).

구체적 diff:

(b-1) Replace line 76-77 변수 정의 + line 107-122 dev/mock else block — `serviceRoleClient` lazy 선언:

```typescript
  // Step 2.7b.2 (R2 MED-2): service-role client는 lazy create per branch. live Naver fetch
  // 실패 / partial failure path는 호출 0건. 동일 branch 내 READ+WRITE는 같은 인스턴스 재사용.
  let serviceRoleClient: SupabaseClient | null = null;
  const naverOn = hasNaverClientId && hasNaverClientSecret;
  let candidates: NewsCandidate[] = [];

  if (naverOn) {
    // 기존 naver fetch path — service-role 미사용, Naver 실패 시 early return 502 유지.
    const results = await Promise.allSettled(
      WATCHLIST_QUERIES.map((w) =>
        fetchNaverNews({ query: w.query, ticker: w.ticker, display: 20 }),
      ),
    );
    const failures = results.filter((r) => r.status === "rejected");
    if (failures.length > 0) {
      return NextResponse.json(
        {
          ok: false,
          mockMode: false,
          error:
            failures.length === results.length
              ? "all Naver news queries failed"
              : "partial Naver news queries failed",
          failedQueries: failures.length,
          totalQueries: results.length,
        },
        { status: 502 },
      );
    }
    const batches = results
      .filter((r): r is PromiseFulfilledResult<NewsCandidate[]> => r.status === "fulfilled")
      .map((r) => r.value);
    const scraped = await scrapeSources([]);
    candidates = dedupeByUrl([...batches.flat(), ...scraped]);
  } else {
    // mock-mode: service-role 1회 생성 (READ fallback 직전 lazy).
    serviceRoleClient = createServiceRoleClient();
    const recent = await getRecentNewsEvents({
      client: serviceRoleClient,
      limit: 50,
    });
    candidates = recent.map((n) => ({
      ticker: n.ticker,
      title: n.title,
      source: n.source,
      url: n.url,
      publishedAt: n.publishedAt,
    }));
  }
```

`import type { SupabaseClient } from "@supabase/supabase-js";` 추가 (top of file).

(b-2) After line 127 (`const classified = ...`), insert WRITE block — live branch는 INSERT 직전 lazy create:

```typescript
  // Step 2.7b.2: classified news_event batch upsert via service-role.
  // R2 MED-2 — live branch는 INSERT 직전 service-role 1회 생성 (Naver 실패 path는 위 early
  // return으로 차단되어 여기 도달 0). mock branch는 위에서 이미 생성.
  if (serviceRoleClient === null) {
    serviceRoleClient = createServiceRoleClient();
  }
  let dbError: string | null = null;
  try {
    await insertNewsEvents(classified, { client: serviceRoleClient });
  } catch (err) {
    dbError = err instanceof Error ? err.message : "news_event_insert_failed:unknown";
  }
```

(c) Response body / status 갱신 (line 142-152):

```typescript
  return NextResponse.json(
    {
      ok: !dbError,
      mockMode: !naverOn,
      fetched: candidates.length,
      classified: {
        critical: criticals.length,
        warning: classified.filter((n) => n.severity === "warning").length,
        info: classified.filter((n) => n.severity === "info").length,
      },
      alertsEmitted: dbError ? 0 : alerts.length,
      dbError,
    },
    { status: dbError ? 502 : 200 },
  );
```

- [ ] **Step 4.4: Run tests to verify they pass**

Run: `cd tudal && npx vitest run src/app/api/cron/news-sweep/__tests__/`
Expected: PASS.

- [ ] **Step 4.5: Commit**

```bash
git add tudal/src/app/api/cron/news-sweep/route.ts tudal/src/app/api/cron/news-sweep/__tests__/
git commit -m "feat(mock-cleanup step 2.7b.2): news-sweep news_event INSERT wiring + 502 dbError"
```

---

### Task 5: 전체 검증 게이트 + scope-guard grep

- [ ] **Step 5.1: 전체 build + lint + test:ci + tsc**

Run: `cd tudal && npm run build && npm run lint && npm run test:ci && npx tsc --noEmit`
Expected:
- build: 25 routes (변경 없음)
- lint: 0 err 5 warn (pre-existing) 변동 없음
- test:ci: **1282 PASS baseline → +N net delta** (omxy R1 LOW/MED fix — 정확 신규 카운트는 helper TDD 8 + route TDD 5 = +13 net, 단 기존 silent-health/news-sweep route test 변경 분 별도. 박제 시 정확 수치보다 "회귀 0 + 신규 모두 PASS" invariant 검증 우선).
- tsc: clean

- [ ] **Step 5.2: scope-guard grep (forbidden patterns 0 매치, omxy R1 MED-2 quote-agnostic + R3 MED multiline fix)**

`ripgrep -U` (multiline mode) 사용 — `supabase\n  .from("...")\n  .upsert(...)` 같은 chained call도 정확 검출. `\s*\.\s*` 사이 whitespace 허용.

```bash
# (a) heartbeat_log INSERT/UPSERT/DELETE는 service-role-injected helper만 사용 — helper-only invariant
# (R4 LOW/MED fix: cron route도 target에 포함 — admin pages 외에도 silent-health route가 helper 거치지 않으면 안 됨)
rg -U -n "from\\([\"']heartbeat_log[\"']\\)\\s*\\.\\s*(insert|upsert|delete)" \
  tudal/src/app/\(admin\)/ tudal/src/components/ \
  tudal/src/app/api/cron/silent-health/route.ts 2>&1
# Expected: no matches (helper insertHeartbeatLog만 사용)

# (b) news_event INSERT/UPSERT/DELETE는 helper만 (route에서 직접 supabase 호출 금지). Quote+multiline-agnostic.
# (R4 LOW/MED fix: cron route 명시 + admin pages도 target에 포함)
rg -U -n "from\\([\"']news_event[\"']\\)\\s*\\.\\s*(insert|upsert|delete)" \
  tudal/src/app/\(admin\)/ tudal/src/components/ \
  tudal/src/app/api/cron/news-sweep/route.ts 2>&1
# Expected: no matches (helper insertNewsEvents 사용)

# (c) service-role client 사용 boundary 위반 (허용 외에서 import) 0
rg -n "createServiceRoleClient" tudal/src/ | rg -v "tudal/src/app/api/cron/|tudal/src/lib/data/admin-batch-runs-cron.ts|tudal/src/lib/supabase/service-role.ts|__tests__"
# Expected: no matches

# (d) Step 2.7b.2 도입 코드에서 admin client 우회 시도 (e.g., supabaseAdmin import / 직접 service-role key 사용) 0
rg -n "supabaseAdmin|SUPABASE_SERVICE_ROLE_KEY|\\.rpc\\(" tudal/src/app/api/cron/silent-health/ tudal/src/app/api/cron/news-sweep/ tudal/src/lib/data/admin-heartbeat-log.ts 2>&1
# Expected: no direct supabaseAdmin/service_role key reference (service-role.ts 한 곳에서만)
# Note: .rpc( grep는 RLS 우회 SQL function 호출 검출용 — 본 step scope는 helper-only UPSERT라 .rpc 호출 0.

# (e) 신규 helpers에 INSERT/UPSERT 가 실제 존재 (positive multiline grep — 코드 fix 후 정확 추가 검증)
rg -U -n "from\\([\"']heartbeat_log[\"']\\)\\s*\\.\\s*upsert" tudal/src/lib/data/admin-heartbeat-log.ts
rg -U -n "from\\([\"']news_event[\"']\\)\\s*\\.\\s*upsert" tudal/src/lib/data/admin-news.ts
# Expected: 1+ match each
```

- [ ] **Step 5.3: Production schema/index/CHECK preflight (omxy R1 LOW + R2 MED-1 fix)**

운영 적용 전 production DB의 (1) unique index, (2) RLS, (3) CHECK constraint 1:1 drift 확인. CHECK drift = STATUS_SET/SEVERITY_SET app-level enum guard와 DB CHECK이 불일치할 때 silent fail-open 가능 (omxy R2 MED-1).

```sql
-- (1) Unique index 정합 (0006/0008 적용 검증)
select indexname, indexdef
from pg_indexes
where schemaname = 'public'
  and indexname in ('heartbeat_log_date_uniq', 'news_event_url_uniq');
-- Expected: 2 rows. 누락 시 마이그 history 0001~0025 chain 확인 후 backfill 결정.

-- (2) RLS 정합 (admin 정책 존재)
select tablename, policyname
from pg_policies
where schemaname = 'public'
  and tablename in ('heartbeat_log', 'news_event')
order by tablename;
-- Expected: heartbeat_log admin all + news_event admin all (각 1 row).

-- (3) CHECK constraint 1:1 drift 검증 (omxy R2 MED-1)
select t.relname as table_name,
       c.conname as constraint_name,
       pg_get_constraintdef(c.oid) as definition
from pg_constraint c
join pg_class t on c.conrelid = t.oid
join pg_namespace n on t.relnamespace = n.oid
where n.nspname = 'public'
  and t.relname in ('heartbeat_log', 'news_event')
  and c.contype = 'c' -- check constraint
order by t.relname, c.conname;
-- Expected:
--   heartbeat_log status_check: status = ANY (ARRAY['ok'::text, 'red_alert'::text])
--   heartbeat_log critical_alert_count_check: critical_alert_count >= 0
--   heartbeat_log warning_alert_count_check: warning_alert_count >= 0
--   news_event severity_check: severity = ANY (ARRAY['critical'::text, 'warning'::text, 'info'::text])
-- drift 시 STATUS_SET/SEVERITY_SET 정의 또는 DB 마이그 hotfix 결정.
```

- [ ] **Step 5.3: Commit (scope-guard 통과 후 별도 commit 불필요 시 skip)**

검증만 — code change 없음.

---

### Task 6: PR create + body draft

- [ ] **Step 6.1: Push branch**

```bash
git push -u origin feat/mock-cleanup-step-2-7b-2-cron-insert
```

- [ ] **Step 6.2: gh pr create**

```bash
gh pr create --base main --head feat/mock-cleanup-step-2-7b-2-cron-insert \
  --title "feat(mock-cleanup step 2.7b.2): cron INSERT 실 path (heartbeat_log + news_event)" \
  --body "$(cat <<'EOF'
## Summary
Step 2.7b.2 — silent-health cron `heartbeat_log` INSERT + news-sweep cron `news_event` INSERT 실 path 가동.

## Architectural decisions (plan §0)
- **D1 heartbeat_log idempotency** = ON CONFLICT (date) DO UPDATE (catch-up retry 정합)
- **D2 news_event idempotency** = ON CONFLICT (url) DO NOTHING (append-only news)
- **D3 retry** = INSERT 자체 retry 없음; Vercel cron daily cadence 의존; INSERT 실패 = 5xx + dbError audit
- **D4 migrations** = 0 (schema 0006/0008 이미 production)
- **D5 helpers** = admin-heartbeat-log.ts (신규) + admin-news.ts (insertNewsEvents 확장)
- **D6 service-role boundary** = 기존 cron/silent-health + cron/news-sweep 정합
- **D7 scope-out** = alert_event INSERT (3-source 통합 Step 2.7b.3) + briefing_log INSERT + portfolio_snapshot 실 SELECT

## Changes
- Create `tudal/src/lib/data/admin-heartbeat-log.ts` — insertHeartbeatLog DI seam + status guard
- Modify `tudal/src/lib/data/admin-news.ts` — insertNewsEvents batch upsert export
- Modify `tudal/src/app/api/cron/silent-health/route.ts` — INSERT wiring + 502 dbError
- Modify `tudal/src/app/api/cron/news-sweep/route.ts` — INSERT wiring + 502 dbError + service-role single-instance
- Modify `tudal/src/lib/supabase/service-role.ts` — boundary 주석 갱신
- New tests: admin-heartbeat-log.test.ts (3) + admin-news-insert.test.ts (5) + silent-health route test 확장 (2) + news-sweep route test 확장 (2) = +12

## Test plan
- [ ] `npm run build` — 25 routes, 변동 없음
- [ ] `npm run lint` — 0 err 5 warn (pre-existing)
- [ ] `npm run test:ci` — 1282 → ~1294 PASS
- [ ] `npx tsc --noEmit` clean
- [ ] scope-guard grep 통과 (plan §Task 5.2)
- [ ] omxy R-debate CONVERGED

## Production verify (post-merge)
- public canary 4/4 OK (`/` `/login` `/macro` `/admin`)
- 인증 세션 canary: `/admin/settings/health` (heartbeat_log 첫 row 적재 후 history 노출 검증)
- production audit: `select count(*) from heartbeat_log` / `select count(*) from news_event` — cron 실행 후 row 적재

## W-defer (follow-up tickets)
- W-cron-alert-event-insert (Step 2.7b.3) — 3-source alert_event INSERT 통합
- W-briefing-log-insert (Step 2.7b.3) — morning-briefing briefing_log INSERT
- W-portfolio-snapshot-real (S7b) — morning-briefing portfolioSnapshot 실 SELECT

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## §3 Self-review checklist (writing-plans skill 정합)

1. **Spec coverage**: HANDOFF "(CLAUDE) Mock cleanup Step 2.7b.2 (cron INSERT 실 path) — silent-health heartbeat_log INSERT + news-sweep news_event INSERT. idempotency / retry / conflict resolution architectural decisions" → Task 1+2 helpers / Task 3+4 wiring / §0 D1~D3 architectural / Task 5 scope-guard 모두 covered. **GAP 0**.
2. **Placeholder scan**: 모든 step이 실제 code/command/expected-output 포함. "TBD" / "TODO" / "fill in" 0 매치.
3. **Type consistency**: `HeartbeatLog` (types/admin.ts) / `HeartbeatStatus` / `NewsEvent` / `Severity` / `SupabaseClient` 모두 import path 일치. `insertHeartbeatLog` 서명은 Task 1.3 정의 = Task 3.4 호출 = 1:1.

## §3.5 omxy R-debate catalog (CONVERGED-R3 candidate plan v3)

R1 + R2 catches catalog + plan v3 fix 매핑:

| # | Round | Severity | 결함 | plan v3 fix 위치 |
|---|---|---|---|---|
| 1 | R1 | HIGH | Task 4.1 — live Naver mode INSERT 미검증 + 기존 test "does not create service-role read client in live mode" 충돌 | §0 D3b 신규 + Task 4 (rename existing test) + Task 4.1 신규 live-mode INSERT test |
| 2 | R1 | HIGH | Task 4.1 — `vi.mock("@/lib/data/admin-news")` 중복 등록 risk | Task 4.1 prelude — 기존 hoisted mock 확장 (`adminNewsMock.getRecentNewsEvents` + `adminNewsMock.insertNewsEvents` 한 block) |
| 3 | R1 | HIGH | Task 3.2 / 4.1 — bare Request without Authorization → 환경 따라 false 401 | Task 3.2 + Task 4.1 — `NextRequest` + 4-way env delete (CRON_SECRET / VERCEL_ENV / NEXT_PUBLIC_APP_ENV) + `NODE_ENV="test"` |
| 4 | R1 | MED | Task 1 — DI fallback 미검증 | Task 1.1 — 신규 test 추가 |
| 5 | R1 | MED | §5.2 grep — single-quote 한정 | §5.2 — Quote-agnostic regex + positive grep |
| 6 | R1 | LOW/MED | §5.1 — test count inconsistent | §5.1 — 정확 수치 박제 폐기 |
| 7 | R1 | LOW | Production schema/index preflight 누락 | §5.3 신규 |
| 8 | R2 | MED | §5.3 preflight — CHECK constraint drift 미검증 | §5.3 (3) 신규 — `pg_constraint` query (severity / status / count >= 0) |
| 9 | R2 | MED | Task 4.3 — serviceRoleClient top-level 생성 = 과한 side effect (Naver 실패 path도 호출) | §0 D3b refinement (lazy per branch) + Task 4.3 (b-1/b-2) lazy create + Task 4.1 — Naver all-fail test (INSERT/service-role NOT called) + live success test 갱신 (createServiceRoleClient toHaveBeenCalledTimes(1)) |
| 10 | R3 | MED | §5.2 grep — quote-agnostic 통과했지만 line-based라 multiline chain (`supabase\n.from(...)\n.upsert(...)`) 검출 미흡 | §5.2 — `rg -U` (multiline) + `\s*\.\s*` whitespace-agnostic regex 적용 (a/b/e). 추가 `.rpc(` grep (d) — RLS 우회 SQL function 호출 검출. |
| 11 | R4 | HIGH | Task 3.2 silent-health mock — `const insertHeartbeatLogMock = vi.fn(); vi.mock(...)` 패턴은 vi.mock hoist 시 factory가 TDZ 변수 참조 → ReferenceError 가능 | Task 3.2 — `const heartbeatLogMock = vi.hoisted(() => ({ insertHeartbeatLog: vi.fn() }))` + `vi.mock(...)`로 hoist-safe |
| 12 | R4 | MED | Task 3.2 신규 sibling describe → 기존 `beforeEach` (serviceRoleMock / getRecentPipelineHealth / getRecentAlertEvents) 미상속 → flaky | Task 3.2 — 기존 `describe("GET /api/cron/silent-health", ...)` **내부**에 추가 (권장) 또는 sibling 시 동일 setup 복제 명시 |
| 13 | R4 | LOW/MED | §5.2 grep target path — helper-only invariant는 admin pages 외 cron route도 검증 필수 | §5.2 (a)(b) — target path에 `tudal/src/app/api/cron/silent-health/route.ts` + `tudal/src/app/api/cron/news-sweep/route.ts` 추가 |

**옵션 reversal 없음** (D1~D7 architectural decisions stand).

---

## §4 omxy R-debate scope guard (Context Packet for next step)

```
task / objective: Mock cleanup Step 2.7b.2 PR plan v1 적대적 검토
base SHA / head SHA / branch: main@15f305c / (plan-only) / no-branch
PR URL: N/A (plan-only)
target files + exact lines: docs/superpowers/plans/2026-05-28-mock-cleanup-step-2-7b-2-cron-insert.md §0 + §2 Task 1~6
allowed scope: plan critique (architectural decisions / TDD invariants / file boundary / scope-out 정합)
out-of-scope:
  - alert_event INSERT 통합 (Step 2.7b.3)
  - briefing_log INSERT (Step 2.7b.3)
  - portfolio_snapshot 실 SELECT (S7b)
  - PR4 / B65 / D11 / S8 / DQ-7 / 멤버 페이지
current invariants:
  - main HEAD 15f305c clean
  - OPEN PR #2 CONFLICTING 보류 only
  - build 25 routes / lint 0 err 5 warn / test:ci 1282 PASS / tsc clean
  - Vercel env PR4_TRIGGER_UPSERT_ENABLED=true + AI_COST_LOG_REAL_INSERT_ENABLED=true
  - 0006 + 0008 production-applied (heartbeat_log + news_event schema 존재)
output mode: catch-only (Complex architectural → CLAUDE.md ⚙️ 정책 정합)
trivial vs complex classification + reason:
  Complex — DB INSERT semantic / idempotency / service-role boundary / cron retry semantics
verification gate: build / lint / test:ci / tsc + plan §5.2 scope-guard grep + canary
stop/escalate conditions:
  - CONVERGED (BLOCKERS 0) after ≤8 rounds
  - ESCALATE if option reversal needed (architectural decisions D1~D7)
```
